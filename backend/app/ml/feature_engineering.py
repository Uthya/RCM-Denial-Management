import pandas as pd


def extract_features(claim_data: dict) -> pd.DataFrame:
    """Transform raw claim data into model features."""
    # Placeholder for feature engineering logic
    return pd.DataFrame([claim_data])
